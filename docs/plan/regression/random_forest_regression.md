# Kế hoạch Random Forest Regression

Mục tiêu: dự đoán `target_temp`.

Các bước chính:

- Dùng dữ liệu trong `data/split`.
- Train trên train, chọn tham số bằng validation.
- Đánh giá MAE, RMSE, R² trên test.
- Lưu kết quả vào `results/regression/temperature/random_forest_regression/`.
