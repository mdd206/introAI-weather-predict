# Lasso Regression

Dự đoán `target_temp` bằng Lasso Regression chạy trên GPU với PyTorch CUDA.

Chạy:

```bash
python src/models/regression/lasso_regression/train.py
```

Kết quả lưu vào `results/regression/temperature/lasso_regression/`.
