# LSTM model

Dùng cho mô hình Deep Learning dự đoán đồng thời:

- `target_temp`: nhiệt độ trung bình ngày mai.
- `target_rain`: ngày mai có mưa hay không.

Script dự kiến: `lstm_multitask/train.py`.

Kết quả nên lưu vào `results/lstm/`.
